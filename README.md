ML Backend
---

Sample ML server, using multiple backends. Currently, supports `ray`.

Roadmap:
- Support AWS, GCP for batch jobs (train and inference)
- Support interactive tasks with `ray serve`
- Add samples to test on clusters
- Add observability: otel, loguru, prometheus/grafana
